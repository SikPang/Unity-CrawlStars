name: Test GitHub Models

on:
  workflow_dispatch:

permissions:
  contents: read
  models: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test inference
        uses: actions/ai-inference@v1
        with:
          model: openai/gpt-4.1-mini
          prompt: "hello"